import { eg } from '@cloudflare/util-en-garde';
export var Organization = eg.object({
  id: eg.string,
  name: eg.string,
  status: eg.string,
  permissions: eg.array(eg.string),
  roles: eg.array(eg.string)
});