import { eg } from '@cloudflare/util-en-garde';
var Scope = eg.object({
  id: eg.string,
  key: eg.string,
  objects: eg.array(eg.object({
    key: eg.string
  }))
});
var DomainGroup = eg.object({
  id: eg.string,
  name: eg.string,
  status: eg.string,
  scope: Scope,
  modified_on: eg.string,
  meta: eg.object({
    editable: eg.string
  }).optional
});